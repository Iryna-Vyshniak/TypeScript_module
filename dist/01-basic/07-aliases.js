"use strict";
/* eslint-disable prettier/prettier */
// Алиас это всё то, что мы обозначаем словом type.
let value;
value = 1; // OK
value = 2; // OK
let answer;
answer = 'yes'; // OK
answer = 'no'; // OK
const userType = {
    name: 'Alexander',
    age: 36,
    skills: ['active', 'tolerance'],
};
const userWithCoords = {
    id: 1,
    name: 'Alice',
    age: 32,
    coords: [10, 20],
};
function fetchWithAuth(url, method) {
    return 1;
}
fetchWithAuth('url', 'GET');
const btn = 'large';
//# sourceMappingURL=07-aliases.js.map